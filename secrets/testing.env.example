# Test-only secrets (peer / subagent Ed25519 key). Copy to secrets/testing.env on the deploy host.
# Not loaded through SDK config — only used by the test suite for a second NEAR identity.
#
# Generate value: base64 -w0 < path/to/second-near-credentials.json
#
# NEAR_TEST_PEER_CREDENTIALS_JSON_B64=
